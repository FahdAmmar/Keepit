# سجل التغييرات (Changelog)

هذا أول ملف `CHANGELOG.md` في المشروع؛ التوثيق يبدأ من هنا فصاعدًا. بنية
الملف مستوحاة من [Keep a Changelog](https://keepachangelog.com/ar/1.0.0/)،
والترقيم يتبع [Semantic Versioning](https://semver.org/lang/ar/).

---

---

## [1.10.0] — اختبارات + فحص أنواع + CI

### 🧪 بنية تحتية هندسية جديدة (دفعة "الصحة الهندسية")

لا تأثير إطلاقًا على الإضافة المشحونة — كل ما يلي أدوات تطوير فقط
(`devDependencies`)، ولم يُلمَس أي ملف تشغيلي فعلي بغرض هذه الدفعة تحديدًا
(الإصلاحات التي طرأت أثناء الفحص، أدناه، استثناء موثَّق).

- **فحص أنواع عبر JSDoc بلا أي خطوة بناء** (`tsc --noEmit` دائمًا) — يستثني
  `main.js`/`index.js` المضغوطتين. البداية بصرامة كاملة كانت 427 خطأ؛
  بعد تخفيف `noImplicitAny` (70% من الأخطاء كانت مجرد نقص تعليقات نوع على
  دوال داخلية، لا أخطاء فعلية — نمط "فحص تدريجي" موصى به رسميًا لمشاريع JS
  قائمة) و`noUncheckedIndexedAccess`، ثم إصلاح الباقي ملفًا بملف: **صفر
  أخطاء** الآن. `types.d.ts` جديد: الشكل القانوني الموحَّد لكل بيانات
  Keepit (`KeepitState`، `KeepitCollection`، `KeepitItem`، `KeepitTrashEntry`
  كاتحاد تمييزي حسب `kind`...)، وإعلانات `self.KeepitXxx` للسكربتات
  الكلاسيكية في `background/*-sw/`.

- **أخطاء حقيقية وُجدت وأُصلحت أثناء الفحص** (لا مجرد إسكات تحذيرات):
  - `local-sync/i18n.js`: نسخة مستقلة من نفس علّة `resolveLocale` التي
    أُصلحت سابقًا في `shared/i18n-engine.js` — قيمة لغة تالفة من التخزين
    كانت تُعاد كما هي بلا أي تحقق فعلي، بدل الرجوع لكشف لغة النظام.
  - `types.d.ts` نفسه أثناء البناء: `KeepitLocalSyncStatus` كان ناقصًا 3
    حقول فعلية (`lastSyncedAt`، `lastPulledAt`، `lastPullError`)،
    و`KeepitSnapshotsPrune` افتُرض خطأً كائنًا يحوي دالة بينما هو دالة
    مباشرة — كلاهما كان سيُربك أي قارئ مستقبلي للتوثيق الضمني الخاطئ.

- **اختبارات Vitest (69 اختبارًا، 6 ملفات)** — بيئة jsdom (لازمة لاختبار
  `netscape-format.js` الذي يستخدم `DOMParser` حقيقيًا، لا محاكاة):
  `shared/dedup.js` (تحميل كسكربت كلاسيكي جانبي-الأثر، يشمل تحقّقًا صريحًا
  من الحفاظ على المعرّف الأصلي عند الدمج)، `local-sync/compare.js`،
  `local-sync/import-schema.js` (يشمل رفض روابط `javascript:` صراحةً)،
  `local-sync/merge-pull.js` (**اختبار مخصَّص للسباق الذي أُصلح في v1.8.0**
  — كتابة متزامنة مُحاكاة تصل أثناء عملية السحب، يتحقق الاختبار أن النتيجة
  النهائية تحتفظ بالتصنيف المُضاف بالتزامن بدل فقدانه)، `bookmarks-bridge/netscape-format.js`،
  `item-manager/store.js`.

- **`scripts/validate-manifest.js`** — يتحقق من بنية `manifest.json`
  الأساسية، تطابق الإصدار مع `package.json`، وأن كل مسار ملف يشير إليه
  (أيقونات، `action.default_popup`، `options_ui.page`، `background.service_worker`)
  موجود فعليًا على القرص — ويتتبّع مراجع `<script>`/`<link>` داخل صفحات
  HTML بدورها. اختُبر عمليًا بإفساد المانفست مؤقتًا والتأكد من رصد الخطأ.

- **GitHub Actions CI** (`.github/workflows/ci.yml`) — فحص أنواع + تحقق
  مانفست + اختبارات على كل push/PR، بأقل صلاحية ممكنة (`permissions:
  contents: read`).

---

## [1.9.0] — إدارة عناصر التصنيف: تحديد متعدد + إجراءات جماعية + إعادة ترتيب

### 🆕 لوحة جديدة: إدارة عناصر التصنيف

زر جديد في شريط أدوات صفحة الإعدادات يفتح لوحة لإدارة مواقع أي تصنيف:

- **تحديد متعدد** — مربع تحديد لكل موقع + "تحديد الكل" (بحالة ثلاثية:
  محدَّد/غير محدَّد/جزئي عبر `indeterminate`).
- **حذف جماعي** — إزالة عادية من مصفوفة العناصر؛ آلية سلة المحذوفات
  الموجودة أصلاً (فرق بالمعرّف، لا الموضع) تكتشفها تلقائيًا وتُنشئ إدخالات
  قابلة للاستعادة بمفردها، بلا أي كود "حذف" إضافي في هذه الميزة.
- **نقل جماعي بين التصنيفات** — مع ملاحظة موثَّقة: النقل يُنشئ إدخال سلة
  محذوفات في التصنيف المصدر أيضًا (سلوك موروث متعمَّد من فلسفة آلية الفرق
  ذاتها؛ لا فقدان بيانات فعلي، إدخال إضافي يُهمَل أو ينتهي تلقائيًا).
- **إعادة الترتيب بمسارين متوازيين**: سحب-وإفلات (HTML5 أصلي) للفأرة/اللمس،
  **وأزرار لأعلى/أسفل** لإتاحة كاملة بلوحة المفاتيح — السحب-والإفلات غير
  قابل للتشغيل بلوحة المفاتيح بطبيعته، فالأزرار مسار كامل موازٍ لا بديل
  تجميلي (نفس مبدأ إمكانية الوصول من v1.6.0).

### 🏗️ قرار معماري: لوحة مستقلة، لا حقن في قائمة main.js

فحصنا الحزمة المضغوطة الأصلية قبل البدء: صفوف المواقع المعروضة فعليًا في
`main.js` لا تحمل أي `data-id` أو معرّف موثوق يمكن الاعتماد عليه من خارجها.
حقن مربعات تحديد أو مقابض سحب داخل تلك الصفوف كان يعني ربطًا هشًّا
بالاسم/الرابط بدل معرّف حقيقي — فبُنيت هذه الميزة بنفس نمط كل لوحة سابقة:
قائمة مستقلة تُدار بالكامل، تقرأ وتكتب `keepit:state` مباشرة.

---

## [1.8.0] — إصلاحات جوهرية + بحث يشمل المواقع + تصدير جماعي

### 🐛 إصلاح جوهري: "مجلدات محذوفة" تظهر في سلة المحذوفات بلا حذف فعلي

**السبب الجذري**: سباق تزامن حقيقي (race condition) في `local-sync/merge-pull.js`
(دورة السحب التلقائية لميزة المزامنة المحلية، تعمل كل دقيقتين بلا أي تدخل
من المستخدم). كانت الدالة تقرأ `keepit:state` مرة واحدة، تحسب الدمج، ثم
تكتب — فإن وصلت كتابة أخرى من سياق مختلف (popup أو options: إضافة موقع،
حذف تصنيف...) بين القراءة والكتابة، كانت كتابة السحب (المبنية على حالة
قديمة) تستبدل تلك الكتابة الطارئة بالكامل، فتُفقَد صامتًا. آلية سلة
المحذوفات (التي تقارن كل قيمة قديمة/جديدة لـ `keepit:state`) كانت ترى ذلك
الفقدان "حذفًا" حقيقيًا وتُسجّله — رغم أن المستخدم لم يحذف شيئًا إطلاقًا.

**الإصلاح**: تحقّق تفاؤلي (optimistic concurrency) — إعادة قراءة الحالة
مباشرة قبل الكتابة، وإعادة حساب الدمج كاملًا من الحالة الجديدة إن تغيّرت
أثناء الحساب، بدل الكتابة فوقها (حلقة محاولات محدودة، 5 كحد أقصى).

### 🐛 إصلاح: اختفاء أزرار السلة/النسخ الاحتياطي/المزامنة/الإشارات المرجعية عند تبديل اللغة

**السبب الجذري**: صفحة الخيارات (`main.js`، الحزمة الأصلية) تُعيد بناء
شريط الأدوات بعنصر DOM **جديد بالكامل** عند تبديل اللغة (عربي/إنجليزي)، لا
تُعدّل العنصر القديم في مكانه. أي زر أُلحق يدويًا بالعنصر القديم (كل
اللوحات الإضافية الأربع) يبقى في الذاكرة لكن ينفصل عن المستند بصمت.

**الإصلاح**: أداة مشتركة جديدة `reattachIfDetached()` في `dom-utils.js`،
تُستدعى من داخل معالج `chrome.storage.onChanged` لـ `keepit:locale`
الموجود أصلاً في كل لوحة (لا مراقب DOM دائم إضافي — صفر كلفة أداء). طُبِّقت
على اللوحات الأربع: سلة المحذوفات، النسخ الاحتياطية، المزامنة المحلية،
وجسر إشارات Chrome (الذي كان يفتقد أيضًا تحديث نص وصفه عند تبديل اللغة —
أُصلح بالمناسبة).

### 🔍 البحث في النافذة المنبثقة يشمل المواقع المحفوظة الآن، لا أسماء التصنيفات فقط

`search-enhance/store.js` كانت تطابق اسم التصنيف فقط. الآن تطابق أيضًا
عنوان ورابط كل موقع بداخله:
- تصنيف يظهر في النتائج إن طابق اسمه **أو** طابق أحد مواقعه.
- تصنيف طابق عبر أحد مواقعه يُوسَّع تلقائيًا ويعرض تلك المواقع تحديدًا (لا
  كل محتواه) — نتيجة بحث دقيقة، لا إغراق. يبقى قابلاً للطيّ اليدوي (منطق
  حالة ثلاثي جديد: طيّ يدوي صريح يتجاوز التوسيع التلقائي).
- تصنيف طابق بالاسم فقط يحتفظ بالسلوك القديم تمامًا (تصفّح كامل محتواه
  يدويًا عند التوسيع).
- حد أقصى 5 مواقع معروضة لكل تصنيف (`MAX_MATCHED_ITEMS_PER_COLLECTION`)
  مع ملاحظة "+N نتيجة أخرى" إن تجاوزها العدد الفعلي.

### 📤 تصدير جماعي لكل التصنيفات إلى إشارات Chrome المرجعية بضغطة واحدة

قائمة اختيار التصنيف في قسم "تصدير إلى إشارات Chrome المرجعية" تتضمن الآن
خيار "كل التصنيفات" في الأعلى، يُصدّر الجميع دفعة واحدة (كل تصنيف = مجلد،
كل موقع بداخله = إشارة مرجعية، بنفس منطق منع التكرار الحالي لكل تصنيف
على حدة).

---

## [1.7.0] — جسر إشارات Chrome المرجعية

### 🔖 ميزة جديدة: استيراد وتصدير مع إشارات Chrome المرجعية

زر جديد في شريط أدوات صفحة الإعدادات يفتح حوارًا بثلاثة مسارات مستقلة،
كل قراءة/كتابة فعلية منها معزولة في `bookmarks-bridge/*.js` (لا تعديل
على `main.js`):

- **استيراد من شجرة `chrome.bookmarks` الحيّة** — اختيار مجلد من قائمة
  منسدلة (بترتيب الشجرة الطبيعي، مع عدد الإشارات لكل مجلد)؛ كل مجلد
  فرعي يحوي إشارات مباشرة يصبح تصنيفًا مستقلاً في Keepit (Keepit لا يدعم
  تصنيفات متداخلة، فالتسطيح هو التمثيل الأمين الوحيد الممكن). دمج آمن
  دائمًا — لا وضع "استبدال" هنا عمدًا؛ استيراد بضع إشارات مرجعية لا يجب
  أن يحذف أي شيء موجود مسبقًا.
- **استيراد/تصدير ملف HTML بصيغة Netscape Bookmark File القياسية** —
  نفس الصيغة التي يفهمها كل متصفح رئيسي (Firefox، Safari، Edge...)،
  للتنقّل بالبيانات خارج نظام Chrome/Keepit بالكامل.
- **تصدير تصنيف Keepit إلى إشارات Chrome المرجعية** — يُنشئ مجلد
  `Keepit` (أو يُعيد استخدامه إن وُجد) تحت شريط الإشارات المرجعية،
  ومجلدًا فرعيًا باسم التصنيف؛ التصدير المتكرر لنفس التصنيف idempotent
  (لا تكرار لإشارة موجودة أصلاً بنفس الرابط بعد التطبيع).

**صلاحية جديدة:** `bookmarks` أُضيفت لـ `manifest.json` — وصول لشجرة
إشارات المتصفح المرجعية فقط (لا علاقة بـ `host_permissions`، التي تبقى
فارغة تمامًا)، ومُستخدَمة حصرًا عند نقرة صريحة، لا في الخلفية. التفاصيل
الكاملة والمبرَّرات في `SECURITY.md`.

**إعادة استخدام مقصودة بدل تكرار منطق:** كلا مصدري الاستيراد (شجرة
Chrome وملف HTML) يمران عبر `toInternalCollections` من
`local-sync/import-schema.js` (نفس التحقق الصارم من بروتوكول الرابط
المطبَّق على الاستيراد اليدوي)، وعبر `KeepitDedup.mergeCollections` من
`shared/dedup.js` (نفس أداة الدمج الآمن المستخدمة في استعادة النسخ
الاحتياطية) — لا محرّك تحقق أو دمج موازٍ ثانٍ في المشروع.

**تصحيح توثيق:** الجولة السابقة (v1.6.0) وثَّقت في `SECURITY.md` أن
أيقونات المواقع تُقرأ عبر نقطة Chrome الداخلية `_favicon/` — فحص أعمق
أثناء هذه الدفعة كشف أن هذا غير دقيق؛ الكود الفعلي يقرأ `tab.favIconUrl`
مباشرة من كائن التبويب عند لحظة الحفظ، بلا أي صلاحية `favicon` أو نقطة
داخلية. جرى تصحيح `SECURITY.md` ليعكس السلوك الفعلي بدقة.

### 🧪 التحقق

كل منطق التحليل (صيغة Netscape HTML غير المنضبطة تاريخيًا)، والدمج،
وتوليد مجلدات/إشارات Chrome (بما يشمل الـ idempotency عند تصدير متكرر)
اختُبِر وظيفيًا عبر محاكاة (jsdom لـ `DOMParser`، ومحاكاة يدوية لـ
`chrome.bookmarks`/`chrome.storage`) قبل الاعتماد عليه — لا افتراضات غير
مُتحقَّق منها حول سلوك تحليل HTML5 الفعلي للصيغة القديمة، ولا حول ترتيب
نتائج قائمة اختيار المجلدات.

---

## [1.6.0] — دفعة إمكانية الوصول والتحصين الأمني

### 🔧 إصلاح جوهري: فقدان تركيز لوحة المفاتيح بعد كل إجراء

كل لوحة من لوحات الميزات الإضافية (سلة المحذوفات، النسخ الاحتياطية،
المزامنة المحلية، ونتائج البحث حسب اسم التصنيف) كانت تُعيد بناء محتواها
بالكامل (`container.replaceChildren()`) بعد أي إجراء — نجاح أو فشل، محليًا
أو قادمًا من `chrome.storage.onChanged`. الزر الذي كان مُركَّزًا (وغالبًا
هو نفس الزر الذي ضغطه المستخدم للتو: استعادة، حذف، مزامنة الآن، تبديل
تفعيل...) كان يُدمَّر ويُستبدل بعنصر DOM جديد تمامًا، فيسقط التركيز صامتًا
إلى `<body>`. النتيجة: مستخدم لوحة المفاتيح أو قارئ الشاشة كان يُعاد به
لأعلى الحوار من جديد بعد **كل** إجراء بلا استثناء — مشكلة إتاحة نظامية
(WCAG 2.4.3)، لا حالة حافة نادرة.

**الإصلاح:** أداة عامة جديدة `rerenderPreservingFocus()` في
`local-sync/dom-utils.js` تلتقط "مفتاح تركيز" (`data-focus-key`) للعنصر
المُركَّز قبل إعادة البناء، وتُعيد التركيز لعنصره المقابل بعدها — أو لأقرب
بديل منطقي داخل نفس الحوار إن لم يعد ذلك العنصر بعينه موجودًا (مثلاً صف
حُذف نهائيًا). طُبِّقت في:

- `options/trash-panel.js`
- `options/snapshots-panel.js`
- `options/local-sync-panel.js` (كل نقاط إعادة الرسم التسع)
- `search-enhance/mount.js` (توسيع/طيّ نتيجة تصنيف)

### ♿ تحسينات إتاحة إضافية

- زر سلة المحذوفات في شريط الأدوات: `aria-label` صار يتضمن العدد الفعلي
  للعناصر ("سلة المحذوفات، 5 عناصر") بدل نص ثابت لا يفصح عن المحتوى
  لمستخدم قارئ الشاشة قبل فتح اللوحة.
- زر تفعيل/تعطيل المزامنة المحلية: أُضيف `aria-pressed` بما إنه عمليًا
  زر تبديل (toggle button) وفق نمط WAI-ARIA APG.
- لوحتا "معاينة الإضافة السريعة" و"نتائج البحث حسب اسم التصنيف": صارتا
  `role="region"` مع `aria-live="polite"` و`aria-labelledby` تشير
  لعنوانيهما — كانتا تظهران وتتحدَّثان بصريًا فقط بلا أي إعلان لقارئ
  الشاشة أثناء الكتابة.
- حقل البحث مربوط الآن بلوحة نتائجه عبر `aria-controls`.

### 🔒 تحصين أمني إضافي (دفاع بالعمق)

- `content_security_policy.extension_pages` صريح الآن في `manifest.json`
  (`script-src 'self'; object-src 'self'; base-uri 'none'; form-action
  'none'; frame-ancestors 'none';`) — يوثّق النية صراحةً ويُسقط
  `'wasm-unsafe-eval'` غير المستخدَمة أصلاً، بدل الاعتماد الضمني على
  افتراضي Manifest V3 فقط.
- ملف [`SECURITY.md`](SECURITY.md) جديد: يوثّق نموذج التهديد، الالتزامات
  الأمنية القائمة فعليًا في الكود، ونموذج الثقة الخاص بالمزامنة المحلية،
  وقناة الإبلاغ عن ثغرات.

### 🧹 تنظيف تقني (بلا أي تغيير في السلوك الظاهر)

- استُخرجت وحدة مشتركة جديدة `shared/item-row.js` (بناء صف "رابط محفوظ":
  أيقونة + عنوان + نطاق) بدل تكرار حرفي كامل لأربع دوال كان موجودًا بين
  `quick-add-preview/mount.js` و`search-enhance/mount.js` — أي تحسين
  مستقبلي على هذا الصف يُطبَّق مرة واحدة فقط الآن.

---

## الإصدارات السابقة

لم تكن مُوثَّقة في ملف مخصَّص قبل هذا الإصدار؛ راجع قسم
["الميزات"](README.md#-features) في `README.md` لنظرة عامة على كل ما
أُنجز حتى v1.5.0 (المجموعات الملوّنة، الحفظ الفوري، الاستيراد/التصدير،
المزامنة المحلية ثنائية الاتجاه، سلة المحذوفات، النسخ الاحتياطية
الدورية، ودعم AR/EN الكامل).
