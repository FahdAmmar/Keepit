# تقرير ميزة ترتيب المجموعات بالسحب والإفلات

## النتيجة

تمت إضافة ميزة **ترتيب يدوي ثابت للمجموعات** إلى واجهتي Keepit الرئيسيتين: النافذة المنبثقة وقائمة المجموعات في صفحة الإعدادات. يظهر الآن مقبض سحب مستقل بجانب كل مجموعة؛ يمكن للمستخدم سحبه لإعادة وضع المجموعة ثم يُحفظ الترتيب مباشرةً. لم يعد التطبيق يعيد فرز المجموعات تلقائيًا بحسب التثبيت أو تاريخ الإنشاء، ولذلك يظل ترتيبك اليدوي كما اخترته.

| الجانب | التنفيذ |
|---|---|
| واجهة المستخدم | مقبض سحب من ست نقاط لكل صف مجموعة، وحالة بصرية أثناء السحب وهدف إفلات واضح. |
| أجهزة الإدخال | السحب بالماوس واللمس والقلم باستخدام Pointer Events؛ وهي واجهة موحّدة لهذه الأنواع من المؤشرات ومتاحة على نطاق واسع عبر المتصفحات الحديثة.[1] |
| الوصول | اضغط `Space` أو `Enter` على المقبض لبدء الترتيب، ثم استخدم الأسهم أو `Home` و`End`، واضغط `Space` أو `Enter` للحفظ أو `Escape` للإلغاء. |
| الاتجاه والتجاوب | تعمل القائمة العمودية في النافذة المنبثقة والقائمة الأفقية/العمودية المتجاوبة في صفحة الإعدادات، مع معالجة صحيحة لاتجاه RTL. |
| الحفظ | يُحفظ الترتيب بترتيب عناصر مصفوفة `collections` نفسها في `chrome.storage.local`، دون تغيير مخطط البيانات أو فقدان أي مجموعة تصل أثناء عملية حفظ متزامنة. |
| المزامنة المحلية | تحتفظ ميزة التصدير/الاستيراد المحلية بتسلسل مصفوفة المجموعات، ولذلك ينتقل الترتيب مع ملف المزامنة عند استخدام وضع الاستبدال. |

> استخدمت الميزة واجهات Pointer Events و`storage.local` القياسية، بدلاً من الاعتماد على سلوك HTML Drag and Drop المتباين على اللمس. توثق MDN أن Pointer Events تقدم نموذجًا موحدًا للماوس والقلم واللمس، كما توثق أن `storage.local` هو واجهة تخزين الامتدادات المناسبة للبيانات الدائمة محليًا.[1] [2]

## الملفات المعدّلة والمضافة

| الملف | الغرض |
|---|---|
| `shared/collection-order-store.js` | طبقة مستقلة لإعادة ترتيب المجموعات وحفظها بكتابة متفائلة آمنة من تعارضات التخزين. |
| `shared/collection-reorder.js` | تفاعل السحب بالمؤشر، البديل الكامل للوحة المفاتيح، والتركيب التلقائي على واجهتي القائمة. |
| `shared/collection-reorder.css` | تصميم المقبض، حالة السحب، مؤشر هدف الإفلات، والحالة الملائمة للوصول. |
| `shared/collection-order-store.test.js` | ستة اختبارات لوحدة حفظ الترتيب، بما فيها حالة وصول مجموعة جديدة بالتزامن. |
| `popup/index.html` و`options/index.html` | تحميل الوحدة والتنسيقات المشتركة في الواجهتين. |
| `popup/main.js` و`options/main.js` و`background/index.js` | إزالة الفرز التلقائي السابق لكي تستخدم الواجهة والنافذة المنبثقة وقائمة السياق ترتيب المصفوفة المحفوظ. |

## التحقق

أجري فحص TypeScript والتحقق من ملف Manifest V3 بنجاح ودون تحذيرات. كما اجتازت مجموعة الاختبارات الكاملة **20 ملف اختبار و199 اختبارًا**. أضيفت ست حالات اختبار جديدة مخصصة لطبقة ترتيب المجموعات، إضافةً إلى اختبار حي في Chromium.

| اختبار الواجهة الحية | النتيجة |
|---|---|
| إنشاء ثلاث مجموعات وتركيب مقابض السحب في النافذة المنبثقة | نجح؛ ظهرت ثلاثة مقابض مترجمة وفق اللغة النشطة. |
| ترتيب المجموعة الأولى بلوحة المفاتيح | نجح؛ تغيّر العرض والتخزين من `Alpha, Beta, Gamma` إلى `Beta, Alpha, Gamma`. |
| سحب المجموعة الأخيرة بالمؤشر إلى الموضع الأول | نجح؛ تغيّر العرض والتخزين إلى `Gamma, Beta, Alpha`. |
| فتح صفحة الإعدادات بعد الحفظ | نجح؛ عرضت القائمة الترتيب نفسه وظهرت ثلاثة مقابض للسحب. |
| أخطاء وقت التشغيل الملتقطة | لا توجد أخطاء. |

## ملاحظة التوافق

تستهدف هذه الحزمة المتصفحات التي تشغّل هذا الامتداد وفق نموذج WebExtensions/Chromium، وبالأخص Chrome وEdge وFirefox الحديث. تعتمد وظيفة السحب على Pointer Events ولا تستخدم واجهة خاصة بمحرك معيّن. أما حفظ ترتيب كل مستخدم فيتم محليًا في `storage.local`، وهي واجهة مبنية على `chrome.storage` وتوفّر إشعار تغيّر لمزامنة الواجهات المفتوحة.[2]

إذا أردت نقل الترتيب بين تثبيتات أو أجهزة مستقلة، فعّل ميزة **المزامنة المحلية** في الإضافة واستخدم وضع الاستبدال عند الرغبة في مطابقة ترتيب ملف المزامنة حرفيًا؛ فالتخزين المحلي وحده لا يرفع البيانات إلى حساب متصفح تلقائيًا.

## طريقة الاستخدام

اسحب مقبض النقاط الست المجاور للمجموعة إلى الموضع المطلوب ثم أفلت. يُحفظ الترتيب تلقائيًا ويظهر مباشرةً في النافذة المنبثقة وصفحة الإعدادات وقائمة السياق. للمستخدمين الذين يعتمدون لوحة المفاتيح، ابدأ بالتركيز على المقبض ثم استخدم مفاتيح الترتيب المذكورة أعلاه.

## المراجع

[1]: https://developer.mozilla.org/en-US/docs/Web/API/Pointer_events "MDN: Pointer events"
[2]: https://developer.mozilla.org/en-US/docs/Mozilla/Add-ons/WebExtensions/API/storage/local "MDN: WebExtensions storage.local"
