/**
 * search-enhance/constants.js
 * ---------------------------------------------------------------------------
 * ثوابت ميزة "البحث يشمل أسماء التصنيفات أيضًا". لا نظير لهذا الملف داخل
 * service worker لأن الميزة بالكامل تعيش داخل صفحات المستند (popup
 * وoptions) فقط — لا حاجة لأي معالجة في الخلفية إطلاقًا.
 */
export const KEEPIT_STATE_KEY = "keepit:state";
export const KEEPIT_LOCALE_KEY = "keepit:locale";

/** مهلة تجميع (debounce) خفيفة لكل ضغطة مفتاح قبل إعادة حساب النتائج —
 *  العملية رخيصة (تصفية مصفوفة في الذاكرة) لكن التأخير البسيط يمنع
 *  "وميض" إعادة رسم غير ضروري أثناء الكتابة السريعة. */
export const DEBOUNCE_MS = 120;

/** أقصى عدد تصنيفات مطابقة نعرضها — القائمة تبقى قابلة للاستخدام ولا
 *  تُغرق نتائج البحث الأساسية (المواقع) الموجودة أصلاً أسفلها. */
export const MAX_MATCHES = 6;
