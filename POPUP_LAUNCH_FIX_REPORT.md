# تقرير إصلاح فتح النافذة المنبثقة

## ما تم إصلاحه

تأكدت من وجود إعداد `action.default_popup` في Manifest، وعدم وجود أي مستمع نقرة على زر شريط الأدوات يتعارض معه. أضفت كذلك حارسًا صغيرًا في الخدمة الخلفية باسم `background/action-popup-sw.js` يعيد تثبيت مسار النافذة المنبثقة إلى `popup/index.html` عند تشغيل المتصفح أو تثبيت/تحديث الإضافة.

هذا يحمي الإضافة من أي قيمة سابقة قد يكون المتصفح قد حفظها لمسار النافذة المنبثقة في ملف المستخدم، ويضمن أن النقر على أيقونة Keepit يفتح الواجهة المعتمدة.

| فحص | النتيجة |
|---|---|
| مسار النافذة المسجل في شريط الأدوات | `chrome-extension://…/popup/index.html` |
| تحميل صفحة النافذة المنبثقة | ناجح |
| أخطاء وقت التشغيل المرصودة | لا يوجد |
| فحص Manifest | ناجح دون تحذيرات |
| الاختبارات الآلية | 22 ملف اختبار و209 اختبارات ناجحة |

## التثبيت

افك ضغط الحزمة، ثم افتح `chrome://extensions`، فعّل وضع المطور، واختر **تحميل إضافة غير مضغوطة**. اختر المجلد المفكوك الذي يحتوي مباشرةً على `manifest.json`، ثم احذف أو عطّل أي نسخة Keepit أقدم قبل تشغيل النسخة الجديدة.
