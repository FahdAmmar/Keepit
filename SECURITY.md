# سياسة الأمان (Security Policy)

هذا الملف يوثّق نموذج التهديد (threat model) والالتزامات الأمنية التي
يتبعها Keepit فعليًا في الكود — لا كوعد تسويقي، بل كمرجع يمكن التحقق منه
مباشرة من المصدر. الهدف أن يكون أي مساهم أو مراجع مستقبلي قادرًا على فهم
"لماذا مصمَّم هكذا" بسرعة، وأن يبقى أي انحراف مستقبلي عن هذه الالتزامات
مكشوفًا بوضوح لا مموَّهًا.

---

## 🎯 نموذج التهديد

Keepit إضافة **محلية بالكامل**: لا حساب، لا خادم، لا اتصال شبكي من أي نوع
في نموذج التشغيل الطبيعي. بناءً على ذلك:

- **لا نثق بأي محتوى واردٍ من صفحات الويب** — الإضافة لا تملك
  `host_permissions` إطلاقًا (`"host_permissions": []` في `manifest.json`)،
  فلا وصول لمحتوى أي موقع تتصفحه. المعلومات الوحيدة المأخوذة من التبويب
  الحالي عند الحفظ (العنوان، الرابط، أيقونة الموقع) تُقرَأ عبر `activeTab`
  فقط عند نقرة صريحة من المستخدم — لا حقن سكربتات، لا قراءة DOM الصفحة.
- **الملف المستورَد (JSON) والملف المُزامَن محليًا يُعامَلان كمدخلات غير
  موثوقة** — يُتحقَّق من بنيتهما (`local-sync/import-schema.js`،
  `local-sync/export-schema.js`) قبل أي دمج مع البيانات الحالية، ولا
  يُنفَّذ أي جزء منهما كسكربت أو HTML.
- **بيانات المستخدم (عناوين، ملاحظات، أسماء تصنيفات) لا تُعرَض أبدًا عبر
  `innerHTML`** — الاستخدام الوحيد لـ `innerHTML` في كل المشروع هو حقن
  أيقونات SVG ثابتة مكتوبة في الكود نفسه (لا قيمة مشتقة من بيانات
  المستخدم أو من ملف مستورَد تمر عبره إطلاقًا). كل نص من المستخدم يُعرَض
  حصرًا عبر `textContent`.

---

## 🔒 الالتزامات الأساسية

| الممارسة | الحالة | التفاصيل |
|---|---|---|
| `host_permissions` | فارغة تمامًا | لا وصول لأي موقع تزوره |
| `eval()` / `new Function()` | غير مستخدَمة إطلاقًا | لا تنفيذ كود ديناميكي في أي ملف |
| سكربتات inline | غير مستخدَمة | كل الكود في ملفات `.js` منفصلة، متوافق مع CSP الافتراضي لـ Manifest V3 |
| تخزين البيانات | `chrome.storage.local` فقط | لا `chrome.storage.sync`، لا رفع لأي حساب Google/Microsoft |
| أيقونات المواقع (favicons) | نقطة Chrome الأصلية `_favicon/` | بدل خدمات خارجية (كـ Google S2) التي تُسرِّب الروابط التي تتصفحها لطرف ثالث؛ راجع `shared/safe-favicon.js` |
| مقبض مجلد المزامنة المحلية | `IndexedDB` الخاص بأصل الإضافة فقط | لا يُخزَّن في `chrome.storage.sync`؛ إذن مستقل لكل متصفح، لا يُرفَع لأي حساب |
| الاعتماديات الخارجية وقت التشغيل | صفر (zero runtime dependencies) | لا حزم npm مُجمَّعة تُنفَّذ في وقت التشغيل عدا الكود المصدري نفسه |

---

## 🛡️ سياسة أمان المحتوى (CSP)

`manifest.json` يحدّد صراحةً:

```json
"content_security_policy": {
  "extension_pages": "script-src 'self'; object-src 'self'; base-uri 'none'; form-action 'none'; frame-ancestors 'none';"
}
```

هذه القيمة **لا تُخفِّف** القيد الافتراضي الذي يفرضه Manifest V3 على
`script-src`/`object-src` (يبقى `'self'` فقط، ولا يمكن تخفيفه أصلاً مهما
كُتب في المانيفست — قيد فرضه المتصفح نفسه). ما تضيفه صراحةً هو:

- إسقاط `'wasm-unsafe-eval'` من `script-src` (موجودة في افتراضي Chrome
  لدعم WebAssembly؛ Keepit لا يستخدم WebAssembly إطلاقًا، فلا داعي لإبقاء
  الباب مفتوحًا لها).
- `base-uri 'none'` — يمنع حقن وسم `<base>` لإعادة توجيه الروابط النسبية
  لأصل مختلف.
- `form-action 'none'` — لا نماذج `<form>` في أي صفحة من صفحات الإضافة
  أصلاً؛ هذا يوثّق النية صراحة بدل تركها ضمنية.
- `frame-ancestors 'none'` — يمنع تضمين أي صفحة من صفحات الإضافة داخل
  `<iframe>` من جهة أخرى. Chrome أصلاً يمنع صفحات خارجية من تضمين موارد
  الإضافة ما لم تُدرَج في `web_accessible_resources` (غير مُعرَّف هنا
  إطلاقًا)، فهذا تحصين إضافي صريح فوق قيد المتصفح الضمني، لا سدّ لثغرة
  قائمة.

الهدف من هذا كله دفاع بالعمق (defense in depth) لا سدّ ثغرة قائمة —
النية أن يبقى أي تراجع مستقبلي عرضي (كإضافة سكربت خارجي بالخطأ) مرفوضًا
من المتصفح نفسه وقت التحميل، لا أن يُكتشف لاحقًا بمراجعة يدوية.

---

## 🧩 المزامنة المحلية: نموذج الثقة تحديدًا

المزامنة المحلية (`local-sync/`) هي الميزة الوحيدة التي تكتب/تقرأ ملفًا
خارج تخزين الإضافة الداخلي، فتستحق توضيحًا خاصًا:

- الوصول عبر **File System Access API** يتطلب موافقة صريحة من المستخدم
  على مجلد بعينه (`showDirectoryPicker`) — لا وصول لأي مسار آخر على
  الجهاز.
- الإذن (`FileSystemDirectoryHandle`) مُخزَّن في IndexedDB **الخاص بأصل
  الإضافة نفسه** (`chrome-extension://<id>`)، ومعزول عن أي إضافة أو موقع
  آخر بحكم Same-Origin Policy.
- الملف المكتوب/المقروء يمر دائمًا عبر التحقق البنيوي في
  `local-sync/import-schema.js` قبل الدمج — ملف تالف أو مُعدَّل يدويًا
  بصيغة غير متوقَّعة يُرفَض بدل أن يُدمَج جزئيًا بصمت.
- لا اتصال شبكي في أي مرحلة من مراحل هذه الميزة — القراءة والكتابة كلتاهما
  محليتان بحتًا على قرص الجهاز.

---

## 📣 الإبلاغ عن ثغرة أمنية

إن اكتشفت ثغرة أمنية في Keepit، الرجاء **عدم فتح Issue علني** على
GitHub يكشف تفاصيلها قبل إصلاحها. بدلاً من ذلك:

1. تواصل مباشرة عبر رسالة خاصة لصاحب المستودع
   ([FahdAmmar](https://github.com/FahdAmmar)) أو عبر ميزة
   [GitHub Security Advisories](https://github.com/FahdAmmar/Keepit/security/advisories)
   الخاصة بالمستودع (إن كانت مفعّلة) — تتيح إبلاغًا خاصًا حتى يجهَّز
   الإصلاح.
2. اذكر: خطوات إعادة الإنتاج، النسخة المتأثرة (`manifest.json → version`)،
   والأثر المحتمل (تسريب بيانات؟ تنفيذ كود؟ تجاوز صلاحيات؟).
3. لا يوجد برنامج مكافآت (bug bounty) رسمي حاليًا، لكن كل تقرير جاد
   سيُنسَب له صراحة في `CHANGELOG.md` عند الإصلاح، ما لم يُطلَب خلاف ذلك.

---

## 🚫 خارج نطاق هذه السياسة

- الثغرات الناتجة عن تحميل نسخة مُعدَّلة يدويًا من الكود المصدري (Keepit
  إضافة مفتوحة المصدر — أي تعديل محلي هو مسؤولية من عدّله).
- ثغرات في المتصفح نفسه (Chrome/Edge/Brave) لا في كود Keepit — تُبلَّغ
  لفريق المتصفح المعني مباشرة.
- هندسة اجتماعية تستهدف المستخدم مباشرة (كإقناعه بمنح إذن مجلد لجهة لا
  يثق بها) — خارج نطاق ما يستطيع كود الإضافة منعه تقنيًا.
