# دليل تثبيت Keepit 1.12.4

هذه الحزمة هي نسخة **غير مضغوطة المصدر** من إضافة Keepit لمتصفحات Chromium المتوافقة، مثل Google Chrome وMicrosoft Edge وBrave. تم إعداد ملف `manifest.json` في جذر الحزمة مباشرة، ولذلك يجب فك ضغط الملف أولًا ثم اختيار هذا المجلد نفسه عند التحميل.

| الخطوة | الإجراء |
|---|---|
| 1 | نزّل ملف `keepit-1.12.4-no-library-tools.zip` وفك ضغطه في مجلد ثابت على جهازك. |
| 2 | افتح صفحة الإضافات: `chrome://extensions` في Chrome/Brave أو `edge://extensions` في Edge. |
| 3 | فعّل **وضع المطوّر** (Developer mode). |
| 4 | اختر **تحميل بدون ضغط** (Load unpacked). |
| 5 | اختر المجلد الذي يحتوي مباشرة على ملف `manifest.json`. |
| 6 | ثبّت إضافة Keepit في شريط الأدوات إذا رغبت، ثم افتحها من أيقونتها. |

> **ملاحظة حول التحديث:** إذا كانت نسخة Keepit موجودة مسبقًا بالطريقة نفسها، فاستبدل مجلدها بالمجلد الجديد، ثم استخدم زر إعادة التحميل في صفحة الإضافات. لا تمسح بيانات الإضافة من صفحة المتصفح؛ إذ تبقى مجموعاتك وروابطك وبياناتك المحلية محفوظة.

## التحقق بعد التثبيت

تأكد من ظهور الإصدار **1.12.4** في بطاقة الإضافة على صفحة الإضافات. يجب أن تفتح النافذة المنبثقة بحجم مريح وأن تبقى أدوات حفظ الروابط، البحث، إدارة المجموعات، السحب والإفلات، ألوان ورموز المجموعات، المزامنة المحلية، سلة المحذوفات، والنسخ الاحتياطية متاحة.

تمت إزالة واجهة وخدمات **أدوات المكتبة** من هذه النسخة؛ لذلك لن يظهر قسمها أو أزرارها أو إجراءاتها في النافذة المنبثقة أو صفحة الإعدادات.
